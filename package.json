{
  "name": "nigredo-astro",
  "version": "1.4.3",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npx esbuild src/scripts/main.js --minify --target=es2017 --outfile=static/main.min.js && astro build && node scripts/update-csp.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.0.0",
    "@fontsource/inter": "^5.2.8",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
